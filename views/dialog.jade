extends layout

block content
	div
		img(src="../images/sciencekit-logo.png", alt="sciencekit logo")

	p(class='auth')
		| <strong> #{ myclient.name }</strong> is requesting access to your account.

	p(class='auth')
		| What do you say?

	form(action="/dialog/authorize/decision", method="post")
		input(name="transaction_id", type="hidden", value="#{ transactionID }")
		div
			button(type="submit", value="Allow", id="allow", style="border: 0px; padding: 0px;")
				span(class="auth")
					| allow
			//input(type="submit", value="Allow", id="allow", style="padding: 14px;")

			button(type="submit", value="Deny", id="deny", style="border: 0px; padding: 0px;")
				span(class="auth")
					| deny
			//input(type="submit", value="Deny", id="deny")